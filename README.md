## Prisma

To apply schema on the database:

```bash
npx prisma db push
```

To generate types of the prisma schema:

```bash
npx prisma generate
```

To view the database:

```bash
npx prisma studio
```

## Practice how objects works
